# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/

# Environment / secrets — never commit real credentials
.env
.env.local
.env*.local
.env.production
.env.staging
.secrets/

# Databases (adapter dev DB)
*.sqlite
*.sqlite3
*.db

# Demo DB snapshots produced by scripts/seeddump.sh (contain data + hashes)
demo-snapshots/

# Logs
logs/
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Test / coverage
coverage/

# OpenEMR instance — never vendor OpenEMR *source* into this repo.
# Anchored to the repo root so this does NOT match the adapter code at
# app/src/lib/data/openemr/ (which must be committed).
/openemr/
/openemr-*/
