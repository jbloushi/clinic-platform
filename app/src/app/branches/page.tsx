import Link from 'next/link'; import { MapPin } from 'lucide-react'; import { PublicShell } from '@/components/domain/public-shell'; import { branches } from '@/lib/clinic-catalog';
export default function Page(){return <PublicShell><div className="mx-auto max-w-5xl px-4 py-12"><p className="text-sm font-semibold text-primary">Branches</p><h1 className="mt-2 text-4xl font-semibold">Care closer to you</h1><div className="mt-8 grid gap-4 sm:grid-cols-2">{branches.map(b=><Link key={b.slug} href={`/branches/${b.slug}`} className="rounded-xl border bg-card p-6 card-hover"><MapPin className="h-6 w-6 text-primary"/><h2 className="mt-4 text-xl font-semibold">{b.name.en}</h2><p className="mt-2 text-sm text-muted-foreground">{b.area.en}</p></Link>)}</div></div></PublicShell>}
