import { StubPage } from '@/components/domain/stub-page';

export default function ReportsPage() {
  return (
    <StubPage
      title="Reports & analytics"
      description="Revenue, utilization, no-show rates."
      detail="Reporting dashboards are planned for Phase 3. Today the dashboard shows the day's numbers; billing lists all payments."
    />
  );
}
