import Link from 'next/link'; import { ArrowRight } from 'lucide-react'; import { PublicShell } from '@/components/domain/public-shell'; import { departments } from '@/lib/clinic-catalog';
export default function Page(){return <PublicShell><div className="mx-auto max-w-5xl px-4 py-12"><p className="text-sm font-semibold text-primary">Clinical departments</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Specialist care, clearly organised</h1><p className="mt-4 max-w-2xl text-muted-foreground">Explore each department, then book with the appropriate service or doctor.</p><div className="mt-8 divide-y rounded-xl border bg-card">{departments.map(d=><Link key={d.slug} href={`/departments/${d.slug}`} className="grid gap-2 p-6 hover:bg-accent sm:grid-cols-[1fr_1.5fr_auto] sm:items-center"><h2 className="font-semibold">{d.name.en}</h2><p className="text-sm text-muted-foreground">{d.summary.en}</p><ArrowRight className="h-4 w-4 text-primary"/></Link>)}</div></div></PublicShell>}
