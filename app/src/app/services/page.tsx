import Link from 'next/link'; import { PublicShell } from '@/components/domain/public-shell'; import { services } from '@/lib/clinic-catalog';
export default function Page(){return <PublicShell><div className="mx-auto max-w-5xl px-4 py-12"><p className="text-sm font-semibold text-primary">Services</p><h1 className="mt-2 text-4xl font-semibold">Choose an appointment type</h1><div className="mt-8 grid gap-4 sm:grid-cols-2">{services.map(s=><Link key={s.slug} href={`/services/${s.slug}`} className="rounded-xl border bg-card p-6 card-hover"><h2 className="font-semibold">{s.name.en}</h2><p className="mt-2 text-sm text-muted-foreground">{s.summary.en}</p>{s.durationMinutes&&<p className="mt-4 text-xs font-semibold uppercase tracking-wide text-primary">About {s.durationMinutes} minutes</p>}</Link>)}</div></div></PublicShell>}
