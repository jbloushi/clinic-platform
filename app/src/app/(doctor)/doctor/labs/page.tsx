import { StubPage } from '@/components/domain/stub-page';
export default function Page() {
  return <StubPage title="Lab requests" description="Order labs and view results." detail="Order writing in OpenEMR needs a custom module — planned for Phase 3. Reads via FHIR are wired up in the patient chart." />;
}
