import { StubPage } from '@/components/domain/stub-page';
export default function Page() {
  return <StubPage title="Imaging requests" description="Order imaging and view reports." detail="Same status as Labs — write path planned for Phase 3, reads visible in the chart." />;
}
